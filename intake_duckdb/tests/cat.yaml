sources:
  tables:
    args:
        uri: "{{URI}}"
    parameters: # User defined parameters
      URI:
        description: duckdb
        type: str
        default: "env(TEST_DUCKDB_URI)"
    description: "points to duckdb database"
    driver: duckdb_cat
    direct_access: forbid
    metadata: {}
